# pose2twist テスト仕様 (Test Specification)

## 単体テスト (gtest)
| ID                | 対応機能 (Feature) | タイトル (Title)                                          | 目的 / 可視化ポイント (Purpose)                                                                         | 前提条件 / セットアップ (Setup)                                                                                                                                                                                     | 入力・操作 (Stimulus)                                                                                     | 期待結果 (Expected Result)                                                                                                                                                                               |
| ----------------- | ------------------ | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UT_POSE2TWIST_001 | F-001              | 並進速度の大きさを距離/時間で算出できる                   | 距離のユークリッドノルムを `linear.x` に格納していることを検証                                          | `Pose2TwistComponent` を継承し `compute_twist` を public で呼び出せる gtest フィクスチャを用意。`previous` と `current` の姿勢を 0.1 s 差で生成し、`previous` は原点、`current` は X=0.2, Y=0.1, Z=0.0 に設定する。 | `compute_twist(previous, current)` を 1 回呼び出す                                                        | `twist.twist.linear.x == sqrt(0.2^2 + 0.1^2)/0.1 ≈ 2.236`、`linear.y` と `linear.z` は 0、角速度も 0。ヘッダ frame_id がコンストラクタで与えた `twist_frame_id` に置き換わる。                           |
| UT_POSE2TWIST_002 | F-002              | 不正な経過時間に対し，0を返す                             | 不正なタイムスタンプに対するフェイルセーフ（警告と0出力）を検証                                         | UT_POSE2TWIST_001 と同じフィクスチャを流用し、`previous` と `current` のタイムスタンプを同一に設定。WARN ログ検証のため `ros_testing::LogSink` を仕込む。                                                           | `compute_twist(previous, current)` を 1 回呼び出す                                                        | 返された `TwistStamped` の線形・角速度がすべて 0。frame_id はパラメータ値に設定される。WARN ログが 1 回発生し、メッセージ内に "non-positive delta time" が含まれる。                                     |
| UT_POSE2TWIST_003 | F-003              | ±π を跨ぐヨー角差分を正規化できる                         | `normalize_angle_difference` により ±π を跨いでも最短角距離で角速度を算出できることを確認               | UT_POSE2TWIST_001 と同じフィクスチャを利用し、`previous` のヨー角を 179 度 (3.124 rad)、`current` を -179 度 (-3.124 rad) に設定。タイムスタンプ差は 0.05 s。                                                       | `compute_twist(previous, current)` を実行                                                                 | `twist.twist.angular.z` が -0.698 rad/s 前後（-2 度 / 0.05 s）になる（許容誤差 ±0.01 rad/s）。その他の角速度は 0。                                                                                       |
| UT_POSE2TWIST_004 | F-004              | 初回の`pose`入力で0出力とキャッシュを行う                 | 最初のメッセージでは速度計算ができないため0を publish し、`previous_pose_` を保存する分岐を検証         | `Pose2TwistComponent` を実ノードとして起動し、`rclcpp::executors::SingleThreadedExecutor` 上でスピン。`twist`/`linear_x`/`angular_z` に対するテストのサブスクライバを QoS KeepLast(10) で用意。                     | `pose` トピックへ単一の `PoseStamped` を publish。                                                        | 3 つの出力トピックがタイムアウト以内 (<=100 ms) に配信され、すべての値が 0。`TwistStamped` のヘッダ frame_id が `twist_frame_id` パラメータ値に置換される。2 回目の publish までは追加の計算がされない。 |
| UT_POSE2TWIST_005 | F-005              | 2件目以降のメッセージでキャッシュ更新とスカラー出力を行う | `previous_pose_` を更新し、`linear_x` と `angular_z` スカラー出力が `TwistStamped` と一致することを確認 | UT_POSE2TWIST_004 と同じ実行環境を継続し、初期化用 `pose` と 0.1 s 後に X=0.1 m / yaw=+0.01 rad へ変化した 2 件目の `pose` メッセージを準備しておく。                                                               | 初期化用 `pose` を publish した後、2 件目の `pose` を publish し、続けて 3 つの出力トピックを取得         | `TwistStamped.twist.linear.x == 1.0 m/s`, `angular.z == 0.1 rad/s`。`linear_x`/`angular_z` の `Float32` 値がそれぞれ double 値と一致し、`TwistStamped` のヘッダスタンプが 2 件目の `pose` と一致する。   |
| UT_POSE2TWIST_006 | F-006              | 上書きした `twist_frame_id` パラメータが即時反映される    | ノードのパラメータ更新が出力に反映されることを確認                                                      | `Pose2TwistComponent` を起動し、`declare_parameter` で `base_link` を既定値に設定。`rclcpp::ParameterEventHandler` を用意し、2 件の `pose` メッセージ（初期化用と測定用）を準備する。                               | `set_parameters` API で `twist_frame_id="odom"` を適用し、初期化用 `pose`、続けて測定用 `pose` を publish | 2 回目の publish で吐き出される `TwistStamped.header.frame_id` が "odom" に変化し、`linear_x`/`angular_z` も引き続き publish されること。                                                                |

## 結合テスト (launch_testing)
| ID                | 対応機能 (Feature) | タイトル (Title)                              | 目的 / 可視化ポイント (Purpose)                                                                                                                    | 前提条件 / セットアップ (Setup)                                                                                                                                                                                                                                                   | 入力・操作 (Stimulus)                                                                                                                      | 期待結果 (Expected Result)                                                                                                                                                                                     |
| ----------------- | ------------------ | --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| IT_POSE2TWIST_001 | F-007              | コンポーネントコンテナ経由での`twist`推定確認 | `pose2twist.launch.py` でコンポーネントコンテナにロードした場合でも QoS や remap が維持され、`TwistStamped` とスカラー出力が想定通りになるかを確認 | `launch_testing_ros` で `pose2twist.launch.py` を既定引数のまま起動し、`/localization/pose_estimator/pose` を入力、`/estimate_twist` を監視できる環境を整える。0.1 s 間隔で 0.2 m の移動を再現する Pose シーケンスを生成する publisher（rosbag2_py または rclcpp ノード）を用意。 | Publisher を起動して 10 サンプル分の Pose シーケンスを publish し、同時に `twist`/`linear_x`/`angular_z` を記録する                        | `TwistStamped.twist.linear.x` が連続して 2.0±0.05 m/s を満たし、yaw の変化がなければ角速度が 0 を維持する。`linear_x` と `angular_z` の Float32 出力も同じ値で購読でき、コンテナログに WARN/ERROR が残らない。 |
| IT_POSE2TWIST_002 | F-006 / F-008      | Launch 引数でのトピック/パラメータ上書き検証  | 実運用時のリマップや frame_id 切り替えが想定通り機能するかを確認。                                                                                 | IT_POSE2TWIST_001 の構成を流用し、launch 引数 `input_pose_topic:=/test/pose`, `output_twist_topic:=/test/twist`, `twist_frame_id:=odom` を指定してノードを起動。`/test/pose` へ publish できるテストノードと `/test/twist` 系のsubscriberを用意。                                 | テストノードから 5個の Pose メッセージのサンプルを `/test/pose` として publish し、`/test/twist`/`/test/linear_x`/`/test/angular_z` を確認 | 受信トピック名が remap 後の `/test/twist` 等へ切り替わる。受信した `TwistStamped.header.frame_id` が常に "odom"。`twist`の値は入力に比例しており、遅延やドロップがない。                                       |
| IT_POSE2TWIST_003 | F-002              | XML Launch と単体ノードのフェイルセーフ       | XML launch を経由した単体ノード実行で不正な時間差に対する警告と出力0が機能するか確認。                                                             | `pose2twist.launch.xml` を `launch_testing_ros` から起動し、`input_pose_topic:=/xml/pose`, `output_twist_topic:=/xml/twist` に設定。`ros2topic` 等で `/xml/twist` ログを取得できるようにしておく。                                                                                | テストノードで 2 個の Pose メッセージを publish し、2 個目の timestamp を 1 ms だけ過去にして `/xml/pose` へ送る                           | `/xml/twist` の受信値がゼロのまま。テストのログ収集で WARN ログ "non-positive delta time" がスロットル周期 (1 Hz) で出る。ノードが停止せずに動作し続ける。                                                     |
