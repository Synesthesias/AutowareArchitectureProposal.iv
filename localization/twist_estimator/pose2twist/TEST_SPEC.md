# pose2twist テスト仕様 (Test Specification)

## 単体テスト (gtest)
| ID                | タイトル (Title)                                       | 前提条件 (Preconditions)                                                                                                   | 入力 (Inputs)                                                                                               | 手順 (Steps)                                             | 期待結果 (Expected Result)                                                                                                                                                     |
| ----------------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| UT_POSE2TWIST_001 | 位置差分から並進速度を計算できる                       | サブクラスで `compute_twist` をテスト用に公開したフィクスチャを生成；`PoseStamped` を 0.1 s 間隔で 2 つ作成                | 先行姿勢は原点，現在姿勢は X 方向に 0.2 m 平行移動；タイムスタンプ差 0.1 s                                  | `compute_twist(previous, current)` を呼び出す            | 返却された `TwistStamped.twist.linear.x` が 2.0 m/s，他の並進成分は 0.0，角速度成分も 0.0                                                                                      |
| UT_POSE2TWIST_002 | 非正の経過時間ではゼロを返す                           | 上記と同じフィクスチャ；`previous` と `current` のタイムスタンプが同一                                                     | 姿勢は同一でタイムスタンプも同一                                                                            | `compute_twist(previous, current)` を呼び出す            | 全成分が 0.0 のツイストが返り，ヘッダの frame id は `twist_frame_id` と一致；警告ログは `rclcpp::Logger::get_logging_interface()` または `ros_testing::LogSink` でアサート可能 |
| UT_POSE2TWIST_003 | ラップアラウンドを跨ぐヨー角差分の正規化               | 同じフィクスチャ；位置は同一，姿勢は ±pi 近傍                                                                              | 先行ヨー 179 度 (3.124 rad)，現在ヨー -179 度 (-3.124 rad)；タイムスタンプ差 0.05 s                         | `compute_twist(previous, current)` を呼び出す            | `twist.twist.angular.z` が およそ -0.698 rad/s（-2 度 を 0.05 s で割った値）となり，最短角距離で計算されることを確認                                                           |
| UT_POSE2TWIST_004 | 初回姿勢受信でゼロのツイストとスカラーを発行           | `Pose2TwistComponent` をエグゼキュータでスピン；`twist` / `linear_x` / `angular_z` に対する `rclcpp::WaitSet` 購読者を用意 | `pose` トピックに 1 つ目の `PoseStamped` を Publish                                                         | 最初に公開された各メッセージを取得                       | 3 つのトピックがタイムアウト内に受信でき，`TwistStamped` の frame id は `twist_frame_id`，速度値はすべて 0.0；スカラートピックも `0.0f` を出力                                 |
| UT_POSE2TWIST_005 | 2 回目の姿勢でキャッシュ更新と期待通りのツイストを出力 | UT_POSE2TWIST_004 と同じ実行時フィクスチャ                                                                                 | 初回は初期化用ポーズを Publish，0.1 s 後に X を 0.1 m，ヨー +0.01 rad だけ変化した 2 回目のポーズを Publish | 次に公開される `twist` / `linear_x` / `angular_z` を取得 | 並進速度の大きさが 1.0 m/s，ヨー角速度が 0.1 rad/s；スカラートピックも一致；`TwistStamped` のヘッダスタンプは 2 回目のポーズと一致                                             |

## 結合テスト (launch_testing)
| ID                | タイトル (Title)                              | 前提条件 (Preconditions)                                              | 入力 (Inputs)                                                                                                                    | 手順 (Steps)                                                          | 期待結果 (Expected Result)                                                                                               |
| ----------------- | --------------------------------------------- | --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| IT_POSE2TWIST_001 | コンポーネントコンテナの E2E ツイスト推定     | `launch_testing_ros` を用いて `pose2twist.launch.py` を既定引数で起動 | 0.1 s の間隔で 0.2 m の移動を再現する bag リプレイまたはスクリプト Publisher                                                     | 起動後に `input_pose_topic` へポーズを流し，`TwistStamped` 出力を収集 | `TwistStamped.twist.linear.x` が 2.0 m/s，角速度はポーズ変化と整合，frame id は `base_link`                              |
| IT_POSE2TWIST_002 | Launch 引数によるパラメータ上書きの確認       | IT_POSE2TWIST_001 と同じ                                              | `twist_frame_id:=odom` を指定して起動                                                                                            | 公開される `TwistStamped` の frame id を検証                          | すべての出力ツイストの frame id が `odom` となり，Launch からのパラメータ伝搬が機能していることを確認                    |
| IT_POSE2TWIST_003 | XML Launch によるスタンドアロン実行の挙動確認 | `launch_testing_ros` から `pose2twist.launch.xml` を使用              | テスト用トピック名（例: `/test/pose`, `/test/twist`）を指定し，タイムスタンプが逆転する 2 つのポーズを投入（2 つ目の時刻が早い） | ログおよび出力トピックを観察                                          | 非正の Δt に関するスロットル警告ログが出力され，公開されるツイストはゼロを維持し，指定したトピック名が反映されていること |
