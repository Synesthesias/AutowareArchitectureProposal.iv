# ekf_localizer テスト仕様 (Test Specification)

## 単体テスト (gtest)
| ID          | 対応機能 (Feature) | タイトル (Title)                               | 目的 / 可視化ポイント (Purpose)                                                                                                        | 前提条件 / セットアップ (Setup)                                                                                                                                                                                                                        | 入力・操作 (Stimulus)                                                                                                                                                                                                                                                                       | 期待結果 (Expected Result)                                                                                                                                                                                     |
| ----------- | ------------------ | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UT_EKFL_001 | F-001              | 遅延補償付き Pose 更新が状態に反映される       | Pose ヘッダ時刻から算出した遅延と `pose_additional_delay` が EKF の過去状態参照に使われることを確認                                    | `ekf_localizer::EKFLocalizerComponent` を派生フィクスチャに埋め込み、`predict_frequency=50 Hz`、`pose_additional_delay=0.0` で起動。`rclcpp::executors::SingleThreadedExecutor` 上でスピンし、`/ekf_pose` を QoS KeepLast(10) で購読できるようにする。 | 1. 原点 Pose を 3 回 publish して状態を安定させる。<br>2. Stamp を `now()-0.08 s` にした Pose (x=5.0,y=0.0,yaw=0) を publish。<br>3. `/ekf_pose` の最新値を取得する。                                                                                                                       | 古い Stamp の Pose でも `ekf_pose.pose.position.x` が 5.0+/-0.05 になり、ヘッダの frame_id が `pose_frame_id` に置き換わる。ログに遅延補償エラーが出ない。                                                     |
| UT_EKFL_002 | F-002              | Mahalanobis ゲートで外れ値を拒否する           | Pose 入力が設定したゲート距離を超えると状態を更新せず WARN を出すことを検証                                                            | `pose_gate_dist=0.2` に設定したノードを同 executor 上で実行し、`/ekf_pose` と rosout を取得できるようにする。最初に有効な Pose (原点) で状態を収束させておく。                                                                                         | 1. 原点 Pose を 5 回 publish。<br>2. 10 m 離れた Pose (x=10,y=0) を publish。<br>3. `/ekf_pose` と rosout を確認。                                                                                                                                                                          | `ekf_pose.pose.position.x` は 0+/-0.05 のまま変化せず、rosout に "mahalanobis distance is over limit" を含む WARN が 1 回出力される。                                                                          |
| UT_EKFL_003 | F-003              | Twist 入力モード切替で利用トピックが一意になる | `use_twist_with_covariance` パラメータで `in_twist` / `in_twist_with_covariance` のどちらを EKF が参照するかを切り替えられることを確認 | テストノードから両トピックへ publish できるようにし、`ekf_localizer` を同プロセスで生成。`~/parameter_events` を購読して再設定の完了を待てるようにしておく。                                                                                           | 1. `use_twist_with_covariance=false` のまま `in_twist.twist.linear.x=1.0` を publish。<br>2. 並行して `in_twist_with_covariance.twist.twist.linear.x=5.0` を publish。<br>3. `/ekf_twist` 値を記録。<br>4. `set_parameters` で `use_twist_with_covariance=true` に変更し、手順1-3を再実施。 | false 時は `/ekf_twist.twist.linear.x` が 1.0+/-0.05 を追従し、true 時は 5.0+/-0.05 を追従する。無効化した入力経路では値が変わらず、警告も出ない。                                                             |
| UT_EKFL_004 | F-004              | Yaw バイアス推定の有無で出力が切り替わる       | `enable_yaw_bias_estimation` を切り替えた際に yaw バイアス出力と bias を含む/含まない Pose の整合が取れているかを確認                  | `enable_yaw_bias_estimation=true/false` をパラメータで制御できるよう 2 ケース実行。Pose 入力に `yaw=+5 deg` と実際の車体 `yaw=0 deg` の不一致を模擬する Twist (wz=0) を publish するテストノードを用意。                                               | 1. `enable_yaw_bias_estimation=true` で Pose を 2 秒間 publish。<br>2. `/ekf_pose`・`/ekf_pose_without_yawbias`・`~/estimated_yaw_bias` を取得。<br>3. ノードを再起動し `enable_yaw_bias_estimation=false` で同手順。                                                                       | 有効時は `~/estimated_yaw_bias` が 約 +0.087 rad に収束し、`ekf_pose` の yaw が `ekf_pose_without_yawbias` より +0.087 rad 大きい。無効時はバイアス出力が常に 0 で、2 つの Pose orientation が完全に一致する。 |

## 結合テスト (launch_testing)
| ID          | 対応機能 (Feature) | タイトル (Title)                                       | 目的 / 可視化ポイント (Purpose)                                                       | 前提条件 / セットアップ (Setup)                                                                                                                                                                                                        | 入力・操作 (Stimulus)                                                                                                                                                           | 期待結果 (Expected Result)                                                                                                                                                                   |
| ----------- | ------------------ | ------------------------------------------------------ | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| IT_EKFL_001 | F-005              | 既定 Launch で推定トピックと TF が継続配信される       | 公式 launch を起動した際に Pose/Twist/TF とデバッグ出力が一貫した周期で流れるかを確認 | `launch_testing_ros` から `launch/ekf_localizer.launch.py` を既定引数で起動。0.05 s 間隔で Pose+Twist を publish するテストノード、`tf2_ros::Buffer` を使った TF リスナ、そして各出力トピックを記録する `rosbag2_py` recorder を準備。 | 1. Launch 描画後 1 s 待機し、Pose/Twist publisher を起動。<br>2. 5 s 間連続 publish。<br>3. Bag/TF ログを検証。                                                                 | `/ekf_pose(*)` と `/ekf_twist(*)` が 50 Hz+/-5 Hz で配信され、`/tf` には `map -> base_link` が存在する。Yaw バイアス関連トピックと `~/debug` も同期間隔で更新され、エラー/WARN が出ない。    |
| IT_EKFL_002 | F-006              | Launch 引数でのトピック/フレーム上書きが即時反映される | Remap と `pose_frame_id` 上書きが入出力に反映されるかを E2E で検証                    | `launch_testing_ros` から `ekf_localizer.launch.py` を `input_pose_topic:=/sim/pose`, `output_pose_topic:=/est/pose`, `pose_frame_id:=odom` 等に変更して起動。Publisher/Subscriber も remap 後の名前で接続する。                       | 1. Remap された Pose/Twist トピックに 3 秒間データを送信。<br>2. `/est/pose` と `/tf` を監視。<br>3. Launch から `pose_frame_id:=map` へ再設定し、同様の publish を追加で実施。 | Remap 後の出力トピックでメッセージが取得でき、`/est/pose.header.frame_id` と TF 親フレームが指定した `pose_frame_id` に切り替わる。トピック名/フレームが Launch 引数変更直後から反映される。 |
