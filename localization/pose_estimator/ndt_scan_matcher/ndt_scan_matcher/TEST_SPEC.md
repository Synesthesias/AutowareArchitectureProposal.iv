# ndt_scan_matcher テスト仕様 (Test Specification)

## 単体テスト (gtest)
| ID           | 対応機能 (Feature) | タイトル (Title)                                  | 目的 / 可視化ポイント (Purpose)                                                                              | 前提条件 / セットアップ (Setup)                                                                                                        | 入力・操作 (Stimulus)                                                                              | 期待結果 (Expected Result)                                                                                           |
| ------------ | ------------------ | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| UT_NDTSM_001 | F-001              | 2件の初期姿勢からセンサ時刻の補間姿勢を生成できる | `getNearestTimeStampPose` と `interpolatePose` がセンサヘッダ時刻に最も近い2件を選び補間結果を返すことを確認 | `PoseWithCovarianceStamped` を 2 件 (t=0.0s, t=0.1s) 用意し、deque へ push。yaw は 0 rad と 0.2 rad に設定。センサ時刻 0.05 s を指定。 | `getNearestTimeStampPose` で old/new ポインタを取得し、`interpolatePose(*old,*new,0.05s)` を呼ぶ。 | 返された姿勢の位置が (0.5*(p_new-p_old)+p_old) に一致し、yaw が 0.1 rad になる。ヘッダ stamp が 0.05 s に変化する。  |
| UT_NDTSM_002 | F-002              | `popOldPose` で古い初期姿勢をキューから除去できる | センサ時刻より過去の姿勢が残らないことを検証                                                                 | t=1.0,1.1,1.2 s の `PoseWithCovarianceStamped` を deque へ push し、センサ時刻 1.15 s を指定。                                         | `popOldPose(deque, 1.15s)` を呼ぶ。                                                                | deque 先頭の stamp が 1.15 s 以上の要素(=1.2 s)のみ残り、サイズが 1 になる。                                         |
| UT_NDTSM_003 | F-003              | `calcTwist` が +/-pi を跨ぐヨー差分を正規化する   | ヨー角が 179deg から -179deg へ変化した場合でも連続した角速度が得られるか確認                                | `PoseStamped` A の yaw=+179deg (3.124 rad)、B の yaw=-179deg (-3.124 rad)、スタンプ差 0.1 s を用意。位置は固定。                       | `calcTwist(A,B)` を呼ぶ。                                                                          | 返る `twist.angular.z` が 約 -0.349 rad/s (=-0.00698 rad / 0.1 s) となり、過渡的な 358deg にならない。線形速度は 0。 |

## 結合テスト (launch_testing)
| ID           | 対応機能 (Feature) | タイトル (Title)                                                         | 目的 / 可視化ポイント (Purpose)                                                                  | 前提条件 / セットアップ (Setup)                                                                                                                                                                                  | 入力・操作 (Stimulus)                                                                                                 | 期待結果 (Expected Result)                                                                                                                                                                              |
| ------------ | ------------------ | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| IT_NDTSM_001 | F-004              | Map・初期姿勢・センサ入力が揃うまで publish を抑制する                   | map, initial pose, sensor の順に入力した際の挙動を確認し、不足要素に対して WARN が出ることを検証 | `launch/ndt_scan_matcher.launch.py` を既定値で起動。`pointcloud_map` publisher (1 回)、`ekf_pose_with_covariance` publisher、`points_raw` publisher、`/diagnostics` watcher を launch_testing ノードで用意。     | 1) map 未投入で sensor cloud を 1 回送信。2) map を送信。3) initial pose を送信。4) sensor cloud を再送。             | 1) `ndt_pose` は publish されず、`"No map pointcloud set."` WARN が 1 Hz で出る。4) では map・pose が揃い `ndt_pose`/`ndt_pose_with_covariance` が publish され、TF `map->ndt_base_link` が生成される。 |
| IT_NDTSM_002 | F-005              | 収束条件に応じて推定結果と診断が切り替わる                               | transform probability と iteration 制約が出力抑制と WARN を引き起こすことを確認                  | Launch を `converged_param_transform_probability:=10.0` で起動。pose/map/sensor publisher を IT_NDTSM_001 と同様に用意。さらに `tf2_ros::Buffer` で TF 取得を監視。                                              | 1) 正常データで sensor cloud を送る。2) `converged_param_transform_probability:=0.5` へ再起動して同じデータを送る。   | 1) `ndt_pose` が publish されず、WARN "NDT did not converge" と diagnostics `skipping_publish_num>0` が確認できる。2) では `ndt_pose`/TF が publish され、診断の state が "Sleeping" になる。           |
| IT_NDTSM_003 | F-006              | `ndt_align_srv` サービスが共分散を使ったモンテカルロ探索で最良姿勢を返す | サービス呼び出しで複数候補から最高スコアの姿勢を返却することを確認                               | Launch を既定値で起動し、`pointcloud_map` と `points_raw` をセット。`PoseWithCovarianceStamped` リクエストを生成するテストクライアントと、リクエストより 0.5 m バイアスした ground truth pose を持つ点群を用意。 | 1) `ndt_align_srv` に初期姿勢共分散 0.5 m, 0.05 rad を与えて呼び出す。2) 応答 pose を TF 経由で ground truth と比較。 | サービス応答の位置誤差が 0.2 m 未満、yaw 誤差が 0.05 rad 未満となり、`transform_probability` が Request より大きい値を示す。`monte_carlo_initial_pose_marker` に最高スコア粒子が表示される。            |
