# pose_initializer テスト仕様 (Test Specification)

## 単体テスト (gtest)
| ID        | 対応機能 (Feature) | タイトル (Title)                                      | 目的 / 可視化ポイント (Purpose)                                                                      | 前提条件 / セットアップ (Setup)                                                                                                                                                                                          | 入力・操作 (Stimulus)                                                   | 期待結果 (Expected Result)                                                                                                                                                                |
| --------- | ------------------ | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UT_PI_001 | F-001              | 地図点群を用いた高さ補正を行える                      | `getHeight` が 1 m 半径内の地図点群と TF を使って z を置き換えることを検証                           | `PoseInitializer` を継承したフィクスチャで `tf2` バッファを差し替え、`pointcloud_map` (frame=`map`) に異なる高さの 3 点を注入。`base_link`→`map` の TF を設定し、入力 pose を `base_link` 座標で z=10 m にする。         | `getHeight(input, output)` を 1 回呼び出す                              | `output.pose.pose.position.z` が地図点群内で最も低い値に置き換わる。x/y は TF を介した結果が反映され、`map_frame_` が地図 header に合わせて更新される。                                   |
| UT_PI_002 | F-002              | 手動 initialpose を NDT で整列しパブリッシュできる    | `handleInitialPose` が高さ補正→疎な共分散→NDT 呼び出し→出力パブリッシュの流れを踏むことを確認        | `PoseInitializer` を実ノードで起動し、`ndt_align_srv` を模擬するテストサーバを用意 (既知の pose を返す)。`initialpose3d` を QoS KeepLast(10) reliable で購読。`pointcloud_map` と TF をセット済み。                      | `/initialpose` へ 1 メッセージをパブリッシュ                            | テストサーバが 1 リクエストを受信し、戻り値が `/initialpose3d` へ転送される。共分散対角成分が `[1, 1, 0.01, 0.01, 0.01, 0.2]` に上書きされ、ヘッダ stamp が入力と一致する。               |
| UT_PI_003 | F-003              | GNSS 初回メッセージのみで初期化しサブスクを停止できる | `handleGnssPose` が 1 件目のみパブリッシュし、`shutdownGnssSubscription` で購読を破棄することを検証  | `use_first_gnss_topic:=true` でノードを起動。`ndt_align_srv` をモック。`initialpose3d` 購読者を用意し、`subscription_count` を監視できるようにフィクスチャで `rclcpp::SubscriptionBase::use_count` を参照。              | `/sensing/gnss/pose_with_covariance` に 2 メッセージを連続パブリッシュ  | 1 件目のみ `/initialpose3d` に出力され、2 件目ではパブリッシュが発生しない。テスト終了時に `gnss_pose_cov` サブスクリプションが破棄されている（購読者数 0、または DEBUG ログが出る）。    |
| UT_PI_004 | F-004              | pose_initializer_srv がトピックも同時に更新する       | サービス経路でも高さ補正・共分散調整・NDT 呼び出し・`initialpose3d` パブリッシュが行われることを確認 | ノードを起動し、`pose_initializer_srv` クライアントをテスト側で用意。`ndt_align_srv` をモックして既知の姿勢を返す。`initialpose3d` をサブスクし、サービス応答とトピックを比較できるようにバッファする。                  | `ros2 service call /pose_initializer_srv ...` 相当で 1 リクエストを送信 | サービス応答 `pose_with_cov` がモックの戻り値と一致し、同じ内容が `/initialpose3d` にパブリッシュされる。失敗時は WARN が出るがテストでは成功 path を確認。                               |
| UT_PI_005 | F-005              | map フレーム変更後も TF とキャッシュが整合する        | 新しい `pointcloud_map` が異なる frame_id を持つ場合でもキャッシュと TF が更新されることを検証       | ノードを起動し、`pointcloud_map` に frame=`map_aligned` を持つ点群をパブリッシュ。`tf2_ros::StaticTransformBroadcaster` で `map_aligned`↔`odom` を登録。`initialpose` を `odom` フレームでパブリッシュできるように準備。 | 地図パブリッシュ後に `/initialpose` へ 1 メッセージを送る               | `getHeight` が `map_aligned` を参照して z を更新し、`/initialpose3d` には TF 変換後の位置が出力される。map フレームを切り替えた WARN が出ず、以降の pose も新フレームを基準に処理される。 |

## 結合テスト (launch_testing)
| ID        | 対応機能 (Feature) | タイトル (Title)                         | 目的 / 可視化ポイント (Purpose)                                                                                              | 前提条件 / セットアップ (Setup)                                                                                                                                                                                                                                       | 入力・操作 (Stimulus)                                                                               | 期待結果 (Expected Result)                                                                                                                                                                                                                                                         |
| --------- | ------------------ | ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| IT_PI_001 | F-006              | NDT Align 未接続時のフェイルセーフを確認 | `pose_initializer.launch.py` 実行時に `ndt_align_srv` が未起動でも高さ補正結果をパブリッシュし続けるフェイルセーフ挙動を検証 | `launch_testing_ros` で `pose_initializer.launch.py` を既定設定で起動 (コンポーネントコンテナ)。`pointcloud_map` をラッチ付きで提供し、TF ツリーを整備。`ndt_align_srv` は起動しない。`/initialpose3d` を購読し、`rclcpp::logging` で WARN を収集するハーネスを組む。 | RViz 相当の `/initialpose` を 1 件パブリッシュ。必要なら 2 件目以降もパブリッシュし連続挙動を観察。 | `/initialpose3d` に高さ補正のみの pose が即座に出力される。ログに "NDT align service is unavailable" と "Timed out while waiting for NDT align service response" が 1 Hz でスロットルされる。ノードはエラーで停止せず、後から NDT ノードを起動すれば自動的にアラインを再試行する。 |
