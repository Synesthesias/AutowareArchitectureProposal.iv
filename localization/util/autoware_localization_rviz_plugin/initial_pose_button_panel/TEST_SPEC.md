# initial_pose_button_panel テスト仕様 (Test Specification)

## 単体テスト (gtest)
| ID          | Feature | タイトル (Title)                                 | 目的 / 可視化ポイント (Purpose)                                                              | 前提条件 / セットアップ (Setup)                                                                                                                                                                                                                                                                            | 入力・操作 (Stimulus)                                                                                                                                                                                 | 期待結果 (Expected Result)                                                                                                                   |
| ----------- | ------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| UT_IPBP_001 | F-001   | Pose 受信までボタンを無効化できる                | GUI ボタンが Pose の初回受信と連動していることを確認                                         | `QApplication` を立ち上げた gtest フィクスチャで `InitialPoseButtonPanel` をインスタンス化。`rclcpp::Node` と `SingleThreadedExecutor` を用意し、テスト専用トピック `/test/pose_a` を購読させる。`rclcpp::Publisher< PoseWithCovarianceStamped >` を同ノードに作成。                                       | 1. `topic_edit_` を `/test/pose_a` に設定。<br>2. パネル初期化直後の `initialize_button_` の `isEnabled()` を取得。<br>3. `/test/pose_a` に任意の Pose を publish し、`executor.spin_some()` を回す。 | 2. の時点では `initialize_button_` が false、3. の後に true。ボタンの表示テキストが "Pose Initializer   Let's GO!" に更新される。            |
| UT_IPBP_002 | F-002   | 最新 Pose をミューテックス付きでキャッシュできる | 複数回のコールバックで `latest_pose_` が上書きされ、サービスリクエストに反映されることを検証 | テスト用に `InitialPoseButtonPanel` を継承した `TestablePanel` を作り、`exposeLatestPose()` と `buildRequestFromCache()` を public で呼べるようにする。`callbackPoseCov` を直接呼び出せるよう `geometry_msgs::msg::PoseWithCovarianceStamped` を 2 種類 (PoseA, PoseB) 用意。                              | 1. PoseA を `callbackPoseCov` に渡す。<br>2. PoseB を 100 ms 後に渡す。<br>3. `buildRequestFromCache()` を呼び、返る `pose_with_cov` を確認。                                                         | PoseB の位置・共分散がそのまま `request.pose_with_cov` に入る。PoseA の情報は残らない。ミューテックス獲得でデータ競合が発生しない。          |
| UT_IPBP_003 | F-003   | サービス成功時にステータスが緑色へ遷移する       | ボタン押下が Pose Initializer へのリクエストと成功表示に直結することを確認                   | `rclcpp::Node` 上に `autoware_localization_srvs::srv::PoseWithCovarianceStamped` のテストサーバを立て、受信したリクエストを即座に成功レスポンスで返す。`InitialPoseButtonPanel` を正規に初期化し、Pose publisher から 1 件 publish 済みの状態にする。Qt のステータスラベル取得には `QSignalSpy` を用いる。 | 1. `initialize_button_` を `QTest::mouseClick` で押下。<br>2. サービスコール完了まで `executor.spin_some()` を回す。                                                                                  | ラベルの文字列が "OK!!!"、StyleSheet が `lightgreen` を含む文字列へ変化する。サービスサーバ側で受信した Pose が publish した内容と一致する。 |

## 結合テスト (launch_testing)
| ID          | Feature | タイトル (Title)                               | 目的 / 可視化ポイント (Purpose)                                                                    | 前提条件 / セットアップ (Setup)                                                                                                                                                                                                                                                                                                                                       | 入力・操作 (Stimulus)                                                                                                                                                                          | 期待結果 (Expected Result)                                                                                                                                                        |
| ----------- | ------- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| IT_IPBP_001 | F-004   | サービス未起動時のフェイルセーフを確認         | 実際の RViz プロセス上でサービス待機タイムアウト時の UI 挙動を検証                                 | `launch_testing_ros` で `rviz2` を `--display-config` (InitialPosePanel を事前に追加した RViz 設定) 付きで起動。Pose publisher ノードのみを同時に立ち上げ、Pose Initializer サービスは起動しない。テスト協調プロセスでは `PySide2` と `rclpy` を使い、RViz プロセスのパネルウィジェットを `QtRemoteObjects` で取得できるよう `QT_QPA_PLATFORM=offscreen` を設定する。 | 1. Pose publisher からメッセージを送信してボタンを有効化。<br>2. テストプロセスから `initialize_button_` をリモートでクリック。<br>3. 5 秒間待機。                                             | 5 秒以内にステータスラベルが "Service Unavailable" を表示し、StyleSheet に `background-color : red` が含まれる。ボタンは自動で再度有効化される。RViz プロセスがクラッシュしない。 |
| IT_IPBP_002 | F-005   | トピック編集による再購読とリクエスト反映を確認 | GUI でトピック名を切り替えた際に購読先とサービスリクエスト内容が切り替わることを end-to-end で確認 | `launch_testing_ros` で RViz、Pose publisher A (`/gnss/pose_a`)、Pose publisher B (`/gnss/pose_b`)、モック Pose Initializer サービスサーバを起動。テスト協調プロセスでは `PySide2.QtTest` を使って `topic_edit_` へ文字列を入力し、サービスサーバ側で受信 Pose を記録する。                                                                                           | 1. Publisher A から PoseA を送信してボタンを有効化。<br>2. `topic_edit_` に `/gnss/pose_b` を入力し Enter。<br>3. Publisher B から PoseB を送信。<br>4. ボタンをクリックしてサービス呼び出し。 | サービスサーバが受信した `pose_with_cov` が PoseB と一致し、PoseA の値は送られない。GUI 側のトピックラベルが `/gnss/pose_b` を表示し、再購読後も遅延なくボタン押下が可能。        |
