# geo_pos_conv テスト仕様 (Test Specification)

## 単体テスト (gtest)
| テスト ID  | 対応機能 (Feature) | タイトル (Title)                               | 目的 / 観点 (Purpose)                                                       | 前提条件 / セットアップ (Setup)                                                                                            | 入力・操作 (Stimulus)                                                                        | 期待結果 (Expected Result)                                                                                                       |
| ---------- | ------------------ | ---------------------------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| UT_GPC_001 | F-001              | プリセットした平面番号で正しい原点を参照できる | 国土地理院の基準値と一致する x/y/z が得られることを確認                     | `geo_pos_conv` を生成し、テスト緯度経度 (35.681236°, 139.767125°) と高度 44.0 m を用意する。                               | `set_plane(9)` を呼び、`llh_to_xyz` を実行後に `x()/y()/z()` を取得する。                    | `x ≈ -35,363.2 m`, `y ≈ -5,992.92 m`, `z = 44.0 m` となり、許容誤差 ±1 mm に収まる。必要に応じてパラメタライズでゾーン毎に確認。 |
| UT_GPC_002 | F-002              | 手動ラジアン原点がプリセットと一致する         | 手動設定した原点がテーブル定義と同一結果を返すことを検証                    | `geo_pos_conv` を生成し、UT_GPC_001 と同じ LLH 入力を用意する。                                                            | `set_plane(0.62831853, 2.44184756)`（北緯36°、東経139°50′）を呼び、`llh_to_xyz` を実行する。 | すべての軸が UT_GPC_001 の結果と ±1 mm 以内で一致し、手動原点が正しく適用される。                                                |
| UT_GPC_003 | F-003              | NMEA 度入力が 10 進度と等価になる              | `set_llh_nmea_degrees` の変換結果が 10 進度経路と等価であるか確認           | `set_plane(9)` を適用済みの `geo_pos_conv` を用意し、NMEA 度 (`lat=3540.87416`, `lon=13946.02750`, 高度 44.0) を準備する。 | `set_llh_nmea_degrees` を呼び、`x()/y()/z()` を記録する。                                    | 3 軸すべてが UT_GPC_001 の結果と ±1 mm 以内に収まり、NMEA→10 進度の前処理が正しい。                                              |
| UT_GPC_004 | F-004              | 高度伝播と平面変換の独立性を確認               | 高度のみ変更しても x/y が不変となり z に直接反映されることを検証            | `set_plane(9)` 済みで LLH を固定した `geo_pos_conv` を用意する。                                                           | `llh_to_xyz` に (35.681236°, 139.767125°, 150.5 m) を入力し、結果を記録する。                | x/y が UT_GPC_001 と ±1 mm 以内で一致し、z が 150.5 m に更新される。                                                             |
| UT_GPC_005 | F-005              | 手動デカルト設定をアクセサで取得できる         | `set_xyz` が例外なく値を保持し、`conv_xyz2llh` が未実装でも安全なことを確認 | 既定コンストラクタで `geo_pos_conv` を生成する。                                                                           | `set_xyz(12.34, 56.78, 9.10)` を呼んだ後に `x()/y()/z()` を読む。                            | 3 つのアクセサが設定値と完全一致し、クラッシュや副作用が発生しない。                                                             |

## 結合テスト (launch_testing)
| テスト ID  | 対応機能 (Feature) | タイトル (Title)                       | 目的 / 観点 (Purpose)                                                            | 前提条件 / セットアップ (Setup)                                                                                                                             | 入力・操作 (Stimulus)                                                                                 | 期待結果 (Expected Result)                                                                                                 |
| ---------- | ------------------ | -------------------------------------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| IT_GPC_001 | F-006              | GNSSPoser 経由の平面座標出力を検証     | `NavSatFix2PLANE` が `geo_pos_conv` の結果と一致し、ジオイド補正が乗ることを確認 | GeographicLib データを `/usr/share/GeographicLib` に配置し、ROS 2 Humble 環境を source。東京駅の `sensor_msgs::msg::NavSatFix` (高=44 m) を入力として用意。 | `NavSatFix2PLANE(msg, 9)` を呼び、返却された `plane` を計測する。                                     | `plane.x ≈ -5,992.92`, `plane.y ≈ -35,363.2` が F-001 の結果と一致し、`plane.z` が EGM2008 による `≈17.6 m` へ補正される。 |
| IT_GPC_002 | F-007              | GeographicLib エラー時のフェイルセーフ | ジオイドデータ欠如などの異常を WARN/ERROR で通知しつつ高度を保持できるか確認     | `GEOGRAPHICLIB_DATA` を未設定、もしくは空ディレクトリに変えて `NavSatFix2PLANE` を呼び出すテストバイナリを用意。                                            | `NavSatFix2PLANE(msg, 9)` を実行し、`rclcpp::Logger` または `ros_testing::LogSink` でログを捕捉する。 | `plane.z` が元の高度 44.0 m を維持し、`ERROR` ログ（"GeographicLib" 含む）が 1 回出力される。ノードは終了しない。          |
| IT_GPC_003 | F-008              | TF コンシューマとの整合性を確認        | `map_tf_generator` など下流ノードと平面座標基準が一致するかを検証                | `geo_pos_conv` の出力を `map_tf_generator` に流す最小 ROS 2 構成を起動し、安定した GNSS シミュレーションを publish。                                        | 変換後の `map->base_link` TF を 5 秒間 `tf2_ros::Buffer` で監視する。                                 | 期待オフセットに対して ±5 cm 以内で安定推移し、TF が途切れない。                                                           |
