# gnss_poser テスト仕様 (Test Specification)

## 単体テスト (gtest)
| テスト ID | 対応機能 (Feature) | タイトル (Title)                              | 目的 / 観点 (Purpose)                                                    | 前提条件 / セットアップ (Setup)                                                                                | 入力・操作 (Stimulus)                                                                             | 期待結果 (Expected Result)                                                                                          |
| --------- | ------------------ | --------------------------------------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| UT-GP-001 | F-001              | `NavSatFix2UTM` が基準値と一致する            | 東京駅付近の LLH を UTM に変換し、東距/北距/正高が基準値と一致するか確認 | `sensor_msgs::msg::NavSatFix` に `lat=35.681236°`, `lon=139.767125°`, `alt=10.0 m` を設定。                    | `NavSatFix2UTM` を呼び、返却された `GNSSStat` を検査。                                            | `coordinate_system==UTM` かつ `zone` が期待 GZD。`x`/`y` がオフライン計算と ±0.01 m、`z` が正高と ±0.05 m 以内。    |
| UT-GP-002 | F-002              | `NavSatFix2MGRS` が指定精度を保持する         | MGRS 変換が 0.1 mm 精度の桁を維持するか検証                              | UT-GP-001 と同じ `NavSatFix` を使用し、`precision=MGRSPrecision::_100MICRO_METER` を指定。                     | `NavSatFix2MGRS` を実行し `GNSSStat` を比較。                                                     | `coordinate_system==MGRS`。GZD が期待通りで、`x`/`y` が UTM 由来の値と ±0.1 m 以内、`z` は UT-GP-001 の正高と一致。 |
| UT-GP-003 | F-003              | 平面直角 (PLANE) 変換の軸入れ替えを確認       | `geo_pos_conv` と同じ結果・軸対応になるか検証                            | `plane_zone=9` を指定し、UT-GP-001 の `NavSatFix` を再利用。                                                   | `NavSatFix2PLANE` を実行し、`geo_pos_conv.llh_to_xyz` の結果と比較。                              | `coordinate_system==PLANE`。`x` が `geo_pos_conv.y()`、`y` が `geo_pos_conv.x()` と ±0.01 m、`z` が正高と ±0.05 m。 |
| UT-GP-004 | F-004              | `UTM2MGRS` の精度スケーリングを確認           | UTM→MGRS 変換で指定精度ごとに桁落ちするか検証                            | `coordinate_system=UTM` の `GNSSStat` (zone=54, northup=true, x=377300.0, y=3946000.0, latitude=35.0) を準備。 | `UTM2MGRS` を `MGRSPrecision::_1_METER` と `_10_METER` で呼び比較。                               | 返却 `GNSSStat` は `coordinate_system==MGRS`、`z` を保持し、`x`/`y` の末尾桁が各精度に応じて丸められる。            |
| UT-GP-005 | F-005              | Orthometric height 変換と例外時フォールバック | EGM2008 による正高化とエラー時のログ/フォールバックを確認                | EGM2008 データを配置した環境で現実的な `NavSatFix` を準備。                                                    | 正常系で `EllipsoidHeight2OrthometricHeight` を実行し、異常系として `latitude=NaN` などを与える。 | 正常時は GeographicLib の結果と ±0.05 m、異常時はエラーログを出し入力高度を返す。                                   |

## 結合テスト (launch_testing)
| テスト ID | 対応機能 (Feature) | タイトル (Title)                              | 目的 / 観点 (Purpose)                                         | 前提条件 / セットアップ (Setup)                                                                                                                           | 入力・操作 (Stimulus)                                                                                 | 期待結果 (Expected Result)                                                                                                                                               |
| --------- | ------------------ | --------------------------------------------- | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| IT-GP-001 | F-006              | FIX 測位で姿勢/TF/共分散を出力できる          | 標準パイプラインが end-to-end で動作するか確認                | 既定パラメータでノードを起動し、`base_link↔gnss` 静的 TF を提供。東向きに 1 m シフトする 2 件の `NavSatFix` (どちらも `STATUS_FIX` と有効共分散) を生成。 | テストノードから 2 メッセージを `fix` に publish。                                                    | 2 件とも `gnss_fixed=true`。`gnss_pose`/`gnss_pose_cov` が `map_frame` で出力され、ヨーは東向き (≈0 rad)。共分散は入力値を維持し、`map→gnss_base_link` TF が更新される。 |
| IT-GP-002 | F-007              | NO_FIX を受けたときに出力を抑止する           | フェイルセーフで姿勢出力が止まるか確認                        | 既定パラメータでノード起動。                                                                                                                              | `status=STATUS_NO_FIX` の `NavSatFix` を 1 件 publish。                                               | `gnss_fixed=false` が配信され、タイムアウト内に `gnss_pose`/`gnss_pose_cov` は出力されない。                                                                             |
| IT-GP-003 | F-008              | `buff_epoch>1` の中央値フィルタを検証         | バッファが満杯になるまで出力を保留し中央値を採用するか確認    | `buff_epoch=5` を launch 引数で指定し、静的 TF を提供。                                                                                                   | ジッタを含む 5 件の `STATUS_FIX` `NavSatFix` を publish。                                             | 最初の 4 件では出力がスキップされ、5 件目で公開。`gnss_pose` の各軸が 5 件の中央値 (±0.01 m) となり、ヨー角は直前中央値との差分から算出される。                          |
| IT-GP-004 | F-009              | 静的 TF 不在時の恒等フォールバックを確認      | TF が欠落しても姿勢を出力し警告するか検証                     | `base_frame->gnss_frame` の静的 TF を配信せずにノード起動。                                                                                               | `STATUS_FIX` の `NavSatFix` を 1 件 publish。                                                         | `gnss_pose` と `gnss_pose_cov` が恒等 TF で出力され、`gnss_fixed=true`。ログに TF 欠如を示す WARN がスロットル付きで記録される。                                         |
| IT-GP-005 | F-010              | 入力共分散 UNKNOWN 時のフォールバック値を確認 | 共分散未定義でも `gnss_pose_cov` が既定対角値を付与するか検証 | 既定パラメータでノード起動。                                                                                                                              | `position_covariance_type=COVARIANCE_TYPE_UNKNOWN`、共分散 0 の `STATUS_FIX` `NavSatFix` を publish。 | `gnss_pose_cov` の位置成分対角 (0,7,14) が 10.0 に置換され、姿勢成分は 0.1/0.1/1.0 のまま。                                                                              |
